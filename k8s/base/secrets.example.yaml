# ⚠️ WARNING: This is a TEMPLATE file for LOCAL DEVELOPMENT ONLY.
# Copy to secrets.yaml and fill in your values.
# NEVER commit secrets.yaml to Git!

apiVersion: v1
kind: Secret
metadata:
  name: uitgo-secrets
  namespace: uitgo
  labels:
    app.kubernetes.io/name: uitgo
type: Opaque
stringData:
  # Database credentials
  POSTGRES_USER: "uitgo"
  POSTGRES_PASSWORD: "CHANGE_ME"
  
  # JWT secrets (generate new ones!)
  JWT_SECRET: "CHANGE_ME_GENERATE_STRONG_SECRET"
  REFRESH_TOKEN_ENCRYPTION_KEY: "CHANGE_ME_MUST_BE_32_BYTES_LONG"
  
  # Internal API key for service-to-service calls
  INTERNAL_API_KEY: "CHANGE_ME_GENERATE_STRONG_KEY"
  
  # Admin credentials (for seeding)
  ADMIN_EMAIL: "admin@example.com"
  ADMIN_PASSWORD: "CHANGE_ME"
  ADMIN_NAME: "Admin"
