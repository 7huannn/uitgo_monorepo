# Sealed Secrets for Dev Environment
# This file is generated by seal-secrets.yml workflow
# DO NOT edit manually - regenerate using the workflow
#
# To regenerate:
#   1. Update secrets in GitHub Secrets
#   2. Run "Seal Secrets" workflow with environment=dev
#   3. The workflow will commit updated sealed-secrets.yaml
---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: uitgo-secrets
  namespace: uitgo
  labels:
    app.kubernetes.io/name: uitgo
    environment: development
spec:
  encryptedData:
    # Placeholder - these values will be replaced by seal-secrets.yml workflow
    # For local development, create a plain Secret manually:
    #   kubectl create secret generic uitgo-secrets \
    #     --from-literal=POSTGRES_USER=uitgo \
    #     --from-literal=POSTGRES_PASSWORD=localdev \
    #     --from-literal=JWT_SECRET=dev-jwt-secret-min-32-characters \
    #     --from-literal=REFRESH_TOKEN_ENCRYPTION_KEY=dev-refresh-key-exactly-32bytes \
    #     --from-literal=INTERNAL_API_KEY=dev-internal-api-key \
    #     --from-literal=ADMIN_EMAIL=admin@uitgo.local \
    #     --from-literal=ADMIN_PASSWORD=adminpassword \
    #     --from-literal=ADMIN_NAME=Admin
    #     -n uitgo
    _PLACEHOLDER: "REPLACE_WITH_SEALED_VALUES"
  template:
    metadata:
      name: uitgo-secrets
      namespace: uitgo
      labels:
        app.kubernetes.io/name: uitgo
        environment: development
    type: Opaque
