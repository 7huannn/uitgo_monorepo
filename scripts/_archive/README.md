# Archived scripts

- `push_and_refresh.sh`: legacy manual GHCR push + ASG instance refresh. Use GitHub Actions/GitOps instead.
- `be_bootstrap.sh`: placeholder backend bootstrap script (never implemented).
- `fe_bootstrap.sh`: placeholder frontend bootstrap script (never implemented).
